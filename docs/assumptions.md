# Assumptions

- Active leads should have an owner and next action.
- Lost leads require a loss reason.
- Enrolled leads have a conversion timestamp.
- Duplicate detection warns on matching phone or email rather than silently merging records.
- Historical users are deactivated rather than deleted.
