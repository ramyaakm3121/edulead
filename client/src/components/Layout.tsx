import{NavLink,Outlet}from'react-router-dom';import{useAuth}from'../lib/auth';
const nav=[['/','Overview','⌂'],['/leads','Leads','◈'],['/tasks','My Tasks','✓'],['/reports','Reports','▥']];
export default function Layout(){const{user,logout}=useAuth();return <div className="app"><aside className="sidebar"><div className="brand"><span className="brandmark">E</span><div><b>EduLead</b><small>Admissions OS</small></div></div><div className="workspace"><span className="avatar">{user?.name?.slice(0,1)}</span><div><b>{user?.name}</b><small>{user?.role?.toLowerCase()}</small></div></div><nav>{nav.map(([to,label,icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>isActive?'active':''}><span>{icon}</span>{label}</NavLink>)}</nav><div className="sidebarBottom"><div className="tip"><b>Daily focus</b><span>Own the next action on every active lead.</span></div><button className="logout" onClick={logout}>↪ Sign out</button></div></aside><main className="main"><header className="topbar"><div><span className="eyebrow">Admissions workspace</span><h2>Good work, {user?.name?.split(' ')[0]}</h2></div><div className="topActions"><span className="rolepill">{user?.role}</span><button className="iconbtn">⌕</button><span className="avatar dark">{user?.name?.slice(0,1)}</span></div></header><div className="content"><Outlet/></div></main></div>}
