import {createContext,useContext,useMemo,useState,type ReactNode} from 'react';import type {User} from '../types';
interface AuthCtx{user:User|null;login:(email:string,password:string)=>Promise<void>;logout:()=>void}
const C=createContext<AuthCtx|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const [user,setUser]=useState<User|null>(()=>{const x=localStorage.getItem('edulead_user');return x?JSON.parse(x):null});const login=async(email:string,password:string)=>{const r=await fetch(`${import.meta.env.VITE_API_URL||'http://localhost:4000/api'}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||'Login failed');localStorage.setItem('edulead_token',j.data.token);localStorage.setItem('edulead_user',JSON.stringify(j.data.user));setUser(j.data.user)};const logout=()=>{localStorage.clear();setUser(null);window.location.href='/login'};return <C.Provider value={useMemo(()=>({user,login,logout}),[user])}>{children}</C.Provider>}
export const useAuth=()=>{const c=useContext(C);if(!c)throw Error('AuthProvider missing');return c};
