import{FormEvent,useState}from'react';import{useAuth}from'../lib/auth';
export default function Login(){const{login}=useAuth();const[email,setEmail]=useState('manager@edulead.local');const[pw,setPw]=useState('Password@123');const[err,setErr]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setErr('');try{await login(email,pw)}catch(x){setErr(x instanceof Error?x.message:'Login failed')}finally{setLoading(false)}}return <div className="login"><div className="loginPanel"><div className="loginBrand"><span className="brandmark">E</span><b>EduLead</b></div><span className="eyebrow">Admissions operating system</span><h1>Turn every enquiry<br/>into a clear next step.</h1><p className="muted">A focused workspace for counsellors and managers to capture, follow up and convert admission leads.</p><div className="loginFeatures"><span>✓ Ownership</span><span>✓ Follow-up discipline</span><span>✓ Conversion visibility</span></div></div><div className="loginCard"><div><span className="eyebrow">Welcome back</span><h2>Sign in</h2><p className="muted">Use your EduLead workspace account.</p></div><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={pw} onChange={e=>setPw(e.target.value)} type="password" required/></label>{err&&<div className="error">{err}</div>}<button className="primary full" disabled={loading}>{loading?'Signing in…':'Sign in to workspace'}</button></form><div className="demo"><b>Demo account</b><span>manager@edulead.local · Password@123</span></div></div></div>}
